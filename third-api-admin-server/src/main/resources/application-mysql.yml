spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/third_api?useSSL=false&characterEncoding=utf8
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  sql:
    init:
      mode: never
